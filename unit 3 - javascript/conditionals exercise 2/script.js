

// The code should output the correct heading title based on the heading number. If the heading number is equal to the number 1 set the heading title to a string of Heading 1 and log it to the console and do the same all the way to 5. If the heading number does not fall into 1 - 5 console log a string saying “There are no headings that match that number”.


// let headingTitle = "";
// let headingNumber = 0;


// if (headingNumber === 1) {
//     console.log(headingTitle = h1);
// } else if ( ) {

// }


//switch statememnt 

let headingTitle = "";
let headingNumber = 0;

switch (headingNumber){
    case 1:
        headingTitle = h1
            break;
    case 2:
        headingTitle = h2
            break;
    case 3:
        headingTitle = h3
            break;
    case 4: 
        headingTitle = h4
            break;

    default: 
        console.log ("there are no headings that match that number")        
}
